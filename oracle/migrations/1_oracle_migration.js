const Oracle = artifacts.require("Oracle");

module.exports = async (deployer, network) => {
//  network = network.toUpperCase()
//  let tokenAddress = process.env[network + '_CHAINLINK_TOKEN_ADDRESS']
//  await deployer.deploy(Oracle, tokenAddress)
//
//  const oracle = await Oracle.deployed()
//  await oracle.setFulfillmentPermission(process.env[network + '_CHAINLINK_NODE_ADDRESS'], true)
};
